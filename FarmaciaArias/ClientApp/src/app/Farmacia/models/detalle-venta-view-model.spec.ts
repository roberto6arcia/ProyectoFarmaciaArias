import { DetalleVentaViewModel } from './detalle-venta-view-model';

describe('DetalleVentaViewModel', () => {
  it('should create an instance', () => {
    expect(new DetalleVentaViewModel()).toBeTruthy();
  });
});
